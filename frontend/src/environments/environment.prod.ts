export const environment = {
  production: true,
  apiUrl: 'http://localhost:3000',  // Update with production URL when deploying
  wsUrl: 'ws://localhost:3000'     // Update with production WebSocket URL when deploying
};